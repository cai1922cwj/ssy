# 智能学习系统 V2 部署说明

## 更新内容

本次更新实现了全新的食物识别学习系统：

### 核心改进
1. **手动纠正功能** - 识别不准确时，用户可以手动输入正确的食物名称
2. **自动归档机制** - 同一食物学习2-3次后自动归档到类别库
3. **类别库对比** - 识别时优先匹配已归档的类别库，提高识别准确率
4. **学习进度追踪** - 可以查看已学习的食物和学习次数

### 新功能
- 我的食物学习库页面
- 学习状态显示（学习中/已归档）
- 类别库筛选功能

## 部署步骤

### 1. 上传文件到 PythonAnywhere

需要上传以下文件：
```
models.py                    # 更新后的模型（新增类别库表）
local_image_recognition.py   # 重写后的识别模块
app.py                       # 新增API接口
food_camera.html             # 更新后的识别页面
learned_foods.html           # 新增的学习库页面
base.html                    # 更新的导航菜单
migrate_v2.py                # 数据库迁移脚本
init_category_libraries.py   # 类别库初始化脚本
```

### 2. 执行数据库迁移

在 PythonAnywhere 的 Bash console 中执行：

```bash
cd ~/your-project-folder
python migrate_v2.py
```

这将创建新的数据库表：
- `category_libraries` - 类别库表
- `learned_foods` - 已学习食物表
- `food_image_samples` - 食物图片样本表
- `food_recognition_logs` - 识别日志表

### 3. 重启应用

在 PythonAnywhere 的 Web 页面点击 **Reload** 按钮。

## 使用说明

### 如何使用新的学习系统

1. **拍照识别食物**
   - 进入"拍照识别食物"页面
   - 上传或拍摄食物照片
   - 查看AI识别结果

2. **手动纠正（关键！）**
   - 如果识别结果不准确，在"识别不准确？手动纠正"区域
   - 输入正确的食物名称
   - 选择食物类别
   - 点击"确认并学习此食物"

3. **学习进度**
   - 同一食物学习2-3次后会自动归档
   - 归档后的食物会优先匹配
   - 可以在"我的学习库"页面查看学习进度

4. **查看学习库**
   - 点击导航菜单"更多" → "我的学习库"
   - 查看所有已学习的食物
   - 按类别筛选

### 识别准确率提升技巧

1. **多次学习同一食物** - 从不同角度、光线拍摄同一食物进行学习
2. **正确选择类别** - 确保选择正确的食物类别
3. **定期查看学习库** - 了解哪些食物已经归档

## 技术说明

### 识别流程
1. 提取图片特征（HSV颜色、亮度、边缘等）
2. 优先匹配用户已学习的类别库
3. 计算特征相似度
4. 返回最可能的食物列表

### 学习流程
1. 用户手动纠正识别结果
2. 保存图片特征到样本库
3. 更新食物的平均特征
4. 样本数>=2时自动归档到类别库

## 故障排除

### 如果识别仍然不准确
- 确保已经学习了该食物2-3次
- 检查学习库中该食物是否已归档
- 尝试从不同角度拍摄学习

### 如果学习失败
- 检查网络连接
- 确保输入了食物名称和类别
- 查看PythonAnywhere错误日志

## 回滚方案

如果需要回滚到旧版本：
1. 恢复旧的 `local_image_recognition.py`
2. 恢复旧的 `food_camera.html`
3. 重启应用

旧的学习数据仍然保留在 `food_image_features` 表中。
